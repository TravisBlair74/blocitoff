<%= current_user.items.each do |item| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= item.title %>
      </h4>
    </div>
  </div>
<% end %>
