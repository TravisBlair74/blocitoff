<h1>Your Account</h1>
<body>
  <% if user_signed_in? %>
    <div>Welcome, <strong><%= current_user.email %></strong></div>
  <% else %>
    <div>You are not signed in.  Please click "Sign Up" or "Sign In" above.</div>
  <% end %>
</body>
